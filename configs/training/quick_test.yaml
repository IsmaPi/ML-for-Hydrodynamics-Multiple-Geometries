strategy: single
batch_size: 32
learning_rate: 0.001
weight_decay: 0.00001
num_epochs: 5
scheduler: cosine
warmup_epochs: 1
gradient_clip: 1.0
seed: 42
log_every: 1
eval_every: 2
checkpoint_every: 3
log_backend: tensorboard
